// Loaded by diag.html as <script type="module" src> and via dynamic import().
// If this runs, the server's .js Content-Type passes strict module MIME checking.
window.__moduleExternalOk = true;
export {};
